import { BrixToAlcoholPipe } from './brix-to-alcohol.pipe';

describe('BrixToAlcoholPipe', () => {
  it('create an instance', () => {
    const pipe = new BrixToAlcoholPipe();
    expect(pipe).toBeTruthy();
  });
});
